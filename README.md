Node.JS
node (REPL)
node --version (conocer la versión)
nvm, fnm: para mas de una versión de node JS, buscar en google github fnm

fnm - - - https://github.com/Schniz/fnm - - : curl -fsSL https://fnm.vercel.app/install | bash
- fnm
- fnm list 
- fnm install 24.13.0 (se utiliza para instalar la version que se desee)

rust lang - - - https://rust-lang.org/ - - : curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

modulos nativos
const os = requiere ('node :os')
- os.platform = nombre del sistema operativo
- os.realease = versión del sistema operativo
 -  os.arch() = Arquitectura
 - os.cpus() = CPUS
 - os.freemem () = memoria libre
 - os.totalmem()

 File System (FS)




